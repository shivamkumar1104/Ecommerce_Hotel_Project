import mongoose from 'mongoose';
import bcrypt from 'bcrypt';

const USerSchema = new mongoose.Schema(
    {
        name: {
            type: String,
            required: 
        }
    }
)
const User = {
    ["name": "String",

    ]
}